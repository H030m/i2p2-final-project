#include "Enemy.hpp"
#include "Connect/RenderSender.hpp" // 需能讀到 TILE_SIZE 等資訊

void UpdateEnemyInstance(EnemyInstance& enemy, float deltaTime, RenderSender& sender) {
    switch (enemy.id)
    {
    case 0:
        /* code */
        break;
    case 1:
        break;
    default:
        break;
    }
}